# Normalize line endings to LF in the repo regardless of platform checkout.
# Prevents CRLF/LF churn when editing on Windows (web/ and src/*.sol).
* text=auto eol=lf

# Binary / lockfile artifacts that must never be normalized or diffed
foundry.lock text=false
*.png binary
*.jpg binary
*.svg text eol=lf